# Données

- **BDD_methodes.csv** — Export des méthodes de contrôle biologique (source : fichier Excel Ecophytopic).
- Les données agrégées pour les graphiques sont définies dans `js/script.js` (objet `chartData`).
